name: Check & deploy NethCTI Middleware API docs

permissions:
  contents: read
  pull-requests: write

on:
  push:
    branches:
      - main
      - master
    paths:
      - nethcti-middleware/doc/openapi.yaml
  pull_request:
    branches:
      - main
      - master
    paths:
      - nethcti-middleware/doc/openapi.yaml
  workflow_dispatch:

jobs:
  deploy-doc:
    if: ${{ github.event_name == 'push' }}
    name: Deploy NethCTI Middleware API docs on Bump.sh
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v6
      - name: Deploy API documentation
        uses: bump-sh/github-action@v1
        with:
          doc: nethcti-middleware
          token: ${{ secrets.BUMP_SH_TOKEN }}
          file: nethcti-middleware/doc/openapi.yaml

  api-diff:
    if: ${{ github.event_name == 'pull_request' }}
    name: Check NethCTI Middleware API diff on Bump.sh
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v6
      - name: Comment pull request with API diff
        uses: bump-sh/github-action@v1
        with:
          doc: nethcti-middleware
          token: ${{ secrets.BUMP_SH_TOKEN }}
          file: nethcti-middleware/doc/openapi.yaml
          command: diff
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
